# Example environment variables for local development
# Copy this to .env and edit values

# Secret key for JWT signing (replace for production)
SECRET_KEY=super_secret_sde_placement_key

# Ports (optional overrides)
GATEWAY_PORT=3000
USER_SERVICE_PORT=4001
ORDER_SERVICE_PORT=4002
